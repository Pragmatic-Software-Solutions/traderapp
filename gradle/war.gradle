apply plugin: "war"

bootWar {
    mainClassName = "biz.pragmatic.TraderappApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
