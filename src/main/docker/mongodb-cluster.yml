version: '2'
services:
  traderapp-mongodb:
    image: mongo:4.2.7
    ports:
      - '27017:27017'
    command: mongos --configdb csvr/traderapp-mongodb-config --bind_ip 0.0.0.0
  traderapp-mongodb-node:
    build:
      context: .
      dockerfile: mongodb/MongoDB.Dockerfile
    command: mongod --shardsvr --replSet rs1
  traderapp-mongodb-config:
    image: mongo:4.2.7
    container_name: traderapp-mongodb-config
    command: mongod --configsvr --dbpath /data/db --replSet csvr
